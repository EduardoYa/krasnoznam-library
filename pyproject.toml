[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "library-web"
version = "1.0.0"
dependencies = [
    "flask>=3.0.0",
    "werkzeug>=3.0.0",
    "gunicorn>=21.0.0",
]
